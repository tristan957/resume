{
  "private": true,
  "scripts": {
    "markdownlint:check": "markdownlint-cli2 '**/*.md' !node_modules",
    "prettier:check": "prettier --config .prettierrc.yml --check '**/*.{md,yml}'",
    "prettier:format": "prettier --config .prettierrc.yml --write '**/*.{md,yml}'"
  },
  "devDependencies": {
    "markdownlint-cli2": "^0.21.0",
    "prettier": "^3.8.1"
  }
}
